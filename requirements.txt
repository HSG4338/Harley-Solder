# HARLEY SOLDER — Python Dependencies
# Install with: pip install -r requirements.txt

# GUI (choose one — PyQt6 preferred)
PyQt6>=6.4.0
# PySide6>=6.4.0  # Alternative — uncomment if using PySide6

# AI (optional — only needed if use_anthropic_api = true in config)
# anthropic>=0.30.0

# TTS (optional)
# pyttsx3>=2.90
